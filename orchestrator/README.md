# orchestrator

Belum di-scaffold. Dijadwalkan di fase AI Assistant (minggu 7–9, lihat `docs/SPEC.md`).

Isi nanti: agent + tool calling, chat streaming (SSE), Reporting Agent. Semua panggilan LLM
lewat `ai-gateway/`, semua data lewat MCP tools → Core API (lihat `CLAUDE.md`).
